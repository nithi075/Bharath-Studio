# Copy this to .env in your React project's root (same level as package.json)
# and change it if your backend runs somewhere else (e.g. after deploying).
VITE_API_URL=https://bs-backend-gegc.onrender.com
